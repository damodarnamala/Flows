  name: Env vars
  on: push
  jobs:
    debug:
      name: Print Github Secret
      runs-on: ubuntu-latest
      steps:
        - name: Check out code
          uses: actions/checkout@v2

        - name: Set up secret file
          env:
            TOP_SECRET: ${{ secrets.TOP_SECRET }}
          run: |
            touch $HOME/file.txt 
            echo $TOP_SECRET >> $HOME/file.txt
        - name: Print file content 
          run: cat $HOME/file.txt 
    