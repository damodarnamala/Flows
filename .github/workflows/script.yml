name: Show Me the S3cr3tz
on: [push]

jobs:
  debug:
    name: Printing TOP_SECRET 
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Set up secret file
      env:
        TOP_SECRET: ${{ secrets.TOP_SECRET }}
      run: |
        touch $HOME/secrets.txt
        echo "Greeting $TOP_SECRET"  >> secrets.txt
        cat $HOME/secrets.txt